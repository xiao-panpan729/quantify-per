# 2026-05-18 变更日志

## PE/HHT 指标化重构（核心）

### 问题
PE 和 HHT 是 cycle_engine/hht_analyzer 中一次性计算的快照，非 per-bar 滚动指标。PE 只做前后两半对比，价格和结构不对齐。

### signal_engine.py（+200行）
- 新增 `_permutation_entropy()` + `_calc_pe_rolling()`：每 bar 60 窗滚动 PE
- 新增 `_calc_hht_daily()`：EMD + Hilbert，每 bar 存瞬时频率/振幅（仅日线）
- 新增 `_calc_cycle_period()`：峰值间距均值（仅 min30）
- CSV 表头 41→47 列：+pe/pe_level/pe_chg_5/hht_freq/hht_amp/cycle_period

### cycle_engine/indicators.py
- analyze_trend_pe 重写 v3：优先读 CSV 的 pe 列（PE 轨迹），分析熵走势而非前后两半

### price_pe_align.py（新建 ~240行）
- 价格阶段检测 × PE 轨迹对齐 × 25 组合矩阵 → 操作提示

## 合成等级系统
- synthesize_report.py（新建 ~454行）：三层聚合引擎
- gen_report_md.py：概览表追加 ABCD 等级

## Bug 修复
- read_bars_lc1 NaN 崩溃修复（int(NaN) → _clamp_u32）
- gen_report_md.py SyntaxError（跨行字符串修复）
- PE 标签体系重命名：趋势强化→顺向蓄力、结构上破→结构突破、结构下破→逆向崩退

## 源码统计
- quantify-per 突破 14,200 行 Python（05-18 更新到 CLAUDE.md）
